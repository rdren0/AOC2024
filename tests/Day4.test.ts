import { findMatches } from "../days/Day4";

describe("Day 4 Tests", () => {
  it("should return the correct result for sample input", () => {
    const input = ["some test input"];
    const result = findMatches(input);
    expect(result).toBe("expected output");
  });
});